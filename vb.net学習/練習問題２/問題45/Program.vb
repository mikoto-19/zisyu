Imports System

Module Program
    Sub Main(args As String())
        Dim price As Integer = 1200
        Dim quantity As Integer = 4

        Dim total As Integer = price * quantity
        Console.WriteLine(total)
    End Sub
End Module
